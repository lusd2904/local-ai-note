# 🔒 安全升级说明文档

## 版本：v1.0.1 安全加固版

---

## 📋 本次升级内容

### 1. ✅ 密码哈希算法升级 (Critical)

**问题**：旧版使用不安全的单次 SHA-256 哈希，容易被暴力破解

**修复**：升级为 PBKDF2-HMAC-SHA256（600,000 次迭代）

**影响范围**：所有使用"笔记加锁"功能的用户

**向后兼容**：
- ✅ 自动兼容旧版密码哈希
- ✅ 验证时自动静默升级为新版（无需用户操作）
- ✅ 不影响现有加密笔记的使用

**技术细节**：
```python
# 旧版（不安全）
SHA-256(salt + password) → 1 次计算，可被 GPU 暴力破解

# 新版（安全）
PBKDF2-HMAC-SHA256(password, salt, 600000 iterations)
→ 600,000 次迭代，抵御暴力破解和 GPU 加速攻击
→ 符合 OWASP 2023 密码存储标准
```

---

### 2. ✅ API 密钥安全加固

**问题**：GET `/api/ai/settings` 接口返回完整 API Key

**修复**：
- ✅ 仅返回遮码版本（如 `sk-...xyz1`）
- ✅ 添加 `api_key_configured` 字段标识是否已配置
- ✅ 完整密钥仅在 POST 更新时接收，GET 时不返回

**影响**：更安全，防止开发者工具泄露密钥

---

### 3. ✅ 笔记克隆安全修复

**问题**：克隆笔记会复制原笔记的密码哈希，导致密码共享

**修复**：
- ✅ 克隆后的笔记自动解除加密
- ✅ 不复制 `password_hash` 字段
- ✅ 用户可对克隆笔记重新设置独立密码

**影响**：避免密码意外共享风险

---

### 4. ✅ 文件上传安全加固

**问题**：批量导入缺少文件大小和数量限制

**修复**：
- ✅ 单次最多导入 100 个文件
- ✅ 单文件最大 10MB
- ✅ 超大文件自动跳过，避免 DoS 攻击

---

## 🚀 升级指南

### 对于现有用户

**无需任何操作！** 系统已自动兼容旧版密码。

当您下次输入密码验证加密笔记时，系统会：
1. 使用旧版算法验证密码（向后兼容）
2. 验证成功后，**自动升级**为新版 PBKDF2
3. 整个过程静默完成，无需用户感知

### 对于开发者

#### 1. 拉取最新代码

```bash
git pull origin main
```

#### 2. 检查数据库中的加密笔记

```bash
cd backend
python migrate_password_hash.py
```

输出示例：
```
🔒 密码哈希安全升级检测脚本
======================================================================

📊 发现 3 篇加密笔记：

  🟡 旧版 SHA-256 - ID: a1b2c3d4... | 标题: 重要凭证
  🟢 新版 PBKDF2 - ID: e5f6g7h8... | 标题: 项目笔记
  🟡 旧版 SHA-256 - ID: i9j0k1l2... | 标题: 私人日记

✅ 系统已自动兼容，用户验证时自动升级！
```

#### 3. 重启服务

```bash
./run.sh restart
```

---

## 🛡️ 安全最佳实践

### 密码强度建议

建议用户使用强密码保护重要笔记：
- ✅ 至少 12 个字符
- ✅ 包含大小写字母、数字和特殊字符
- ✅ 避免使用常见密码（如 `password123`）
- ✅ 每个重要笔记使用不同的密码

### 数据备份建议

```bash
# 定期备份数据
./run.sh backup

# 备份文件保存在 backups/ 目录
# 格式：backup_YYYYMMDD_HHMMSS.tar.gz
```

---

## 📊 性能影响评估

### PBKDF2 性能开销

| 操作 | 旧版 SHA-256 | 新版 PBKDF2 | 增加耗时 |
|------|-------------|------------|---------|
| 加密（设置密码） | ~0.1ms | ~500ms | +499.9ms |
| 验证（输入密码） | ~0.1ms | ~500ms | +499.9ms |

**说明**：
- 验证密码时增加 ~500ms 延迟（600,000 次迭代）
- 这是**故意设计**，用于抵御暴力破解攻击
- 对用户体验影响极小（仅在输入密码时发生）
- 攻击者每次尝试也需要 500ms，暴力破解成本提高数百万倍

---

## 🔬 技术对比

### SHA-256 vs PBKDF2

| 特性 | SHA-256 | PBKDF2 |
|------|---------|--------|
| **计算速度** | 极快（~1 GB/s） | 慢（设计如此） |
| **GPU 加速** | 支持（攻击优势） | 受限（防御优势） |
| **暴力破解成本** | 低 | 极高 |
| **迭代次数** | 1 次 | 600,000 次 |
| **OWASP 推荐** | ❌ 不推荐 | ✅ 推荐 |
| **安全性** | 🔴 低 | 🟢 高 |

**现实案例**：
- SHA-256：单张 RTX 4090 每秒可尝试 ~50 亿次
- PBKDF2（600K 迭代）：同样硬件每秒仅能尝试 ~2,000 次
- 破解效率降低 **250 万倍**

---

## 🐛 已知问题

### 1. 无法自动检测所有旧版哈希

**原因**：旧版和新版哈希格式完全相同（都是 `salt$hash`）

**影响**：迁移脚本无法 100% 准确区分

**解决方案**：首次验证时自动升级，无实际影响

---

## 📞 技术支持

如有任何问题，请：
1. 查看项目 README
2. 提交 GitHub Issue
3. 查看运行日志 `./run.sh logs`

---

## ✅ 测试清单

在生产环境部署前，请确认：

- [ ] 旧版加密笔记能正常验证
- [ ] 验证后密码自动升级为新版
- [ ] 新创建的加密笔记使用新版哈希
- [ ] 克隆笔记不会复制密码
- [ ] API 设置接口不返回完整密钥
- [ ] 批量导入文件有大小限制

---

**升级完成后，您的笔记系统安全性已提升到企业级标准！** 🎉

---

_最后更新：2026-08-19_
