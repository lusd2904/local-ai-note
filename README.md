# Local AI Note - 智能笔记与智库工作站

## 项目概述
Local AI Note 是一款专为隐私与效率打造的本地私有化 AI 智能笔记与智库工作站。数据完全物理留存在本地，结合现代化所见即所得编辑器、AI 智能助手与深度内容解析，为您提供安全、高效的知识管理与生产力体验。

## 🌟 核心功能一览

- 📝 **现代化块级富文本与 Markdown 所见即所得编辑**：BlockNote 驱动（兼容历史 Tiptap JSON），支持斜杠命令、待办/表格/代码块、系统截图（Cmd+Shift+4）秒级 Cmd+V 粘贴渲染并持久化存储、支持 Word (.docx) 一键导出与本地备份。
- 🔒 **笔记二次验证与密码锁定**：重要笔记支持一键加锁，PBKDF2 加盐哈希加密，列表卡片强制脱敏保护，会话级解密内存隔离，彻底杜绝敏感凭证泄露。
- 🎙️ **语音录音工坊 (Audio Studio)**：高保真音频采集，波形可视化，Whisper 语音转写，AI 自动提炼核心结论与待办清单 (Action Items)，一键转入笔记。
- ✨ **AI 智能扩写与快捷算子**：一键深度扩写（根据简略提纲检索补充行业背景与权威数据）、智能摘要、专业润色、标签自动提取、Mermaid 思维导图一键生成。
- 🤖 **独立 AI 智能咨询助手**：全尺寸沉浸式咨询工作区，支持 Claude 3.7 (Extended Thinking)、DeepSeek R1、OpenAI GPT-4o、Ollama 离线多模型自由切换，咨询结果一键沉淀为笔记。
- 🌐 **官方网页版 AI 深度内嵌**：字节跳动「豆包」与「DeepSeek」官方网页版原生集成，基于 macOS 原生顶级 WKWebView 架构，登录态系统级持久化存储，彻底告别重复登录与 iframe 跨域拦截。
- 📂 **无限多级笔记本与标签系统**：树状目录无限层级、递归删除与移动、收藏夹、废纸篓与暗黑模式支持。
- 🍏 **原生 macOS 桌面客户端 (`Note.app`)**：Swift + WebKit 构建，沉浸式毛玻璃无边框窗口、独立 Dock 图标、原生麦克风权限支持；服务已就绪时秒开无 Docker 过渡页，原生菜单 / 快捷键与窗口主题状态记忆。
- 📱 **iOS 独立端与局域网免公网双向同步**：iOS 端完全脱离 macOS 独立运行，数据物理存储在 iPhone 本地沙盒；支持在同一 Wi-Fi 局域网下通过动态二维码/PIN 码一键配对，秒级双向增量数据同步。
- 🔗 **块级双向链接与反向引用 (Backlinks)**：支持 `[[双链]]` 快速关联已有笔记，正文内链一键跳转，底部抽屉自动汇总反向提及与上下文摘要。
- 🪐 **交互式 2D 力导向知识关系图谱 (Graph View)**：原生 60fps Canvas 物理模拟引擎，星空全景漫游、手势/滚轮缩放、节点拖拽聚类与点击定位。
- 🟩 **GitHub 风格 365 天知识产出打卡热力图 (Activity Heatmap)**：量化每日记录与字数沉淀，活跃天数与连续打卡统计，激发持续记录习惯。
- 📊 **Notion 式多维数据库表 (Database & Kanban)**：结构化数据表 + 强类型属性系统 + 双视图（表格/看板）自由切换 + 行即页面详情抽屉，支持底部汇总统计与多维度筛选。

## 🚀 0822 V9 迭代更新日志 (BlockNote 块级编辑器、列表热路径与同步查询优化)

### 1. ✍️ 富文本编辑器升级为 BlockNote
- **Notion 式块级编辑**：斜杠命令 `/` 插入标题、列表、待办、表格、代码块与图片，选区浮动工具条与块拖拽侧栏开箱即用。
- **旧笔记平滑迁移**：优先读取 BlockNote JSON；历史 Tiptap `content_json` 自动转 Markdown 再导入，搜索/双链/导出仍以 Markdown 落库。
- **AI 算子补齐**：续写、格式化、纠错、多风格润色真正回写正文；咨询结果通过编辑器桥接插入，不再清空结构化 JSON。
- **暗色模式与阅读模式**：跟随系统主题切换，阅读态只读预览。

### 2. ⚡ 列表、数据表与图谱热路径
- 笔记列表只取摘要预览，不再把全文/`content_json` 拉进内存；侧栏计数一次聚合。
- 数据表侧栏改为 `include_rows=false` + `row_count`，打开详情才加载行。
- 列表刷新并行请求；废纸篓数据表仅在废纸篓视图拉取。
- 重视图按需懒加载；知识图谱 hover 不再打断 60fps 动画循环。

### 3. 🗄️ 后端查询与稳定性
- 组合索引 + SQLite cache/temp 调优；批量导入单事务提交。
- 同步合并改为批量 IN 查询，锁定笔记不再明文下发正文。
- 锁定笔记禁止未解锁导出；反向引用先用 SQL LIKE 预筛。
- 录音后台任务自开 Session，修复 Whisper 客户端误 `await`。

---

## 🚀 0821 V8 迭代更新日志 (多维数据表自建标签、密码安全校验、AI标签提炼、macOS原生窗口拖拽与导入增强)

### 1. 📊 Notion 式多维数据表自建标签与体验升级 (Database 2.0)
- **自建标签与手动维护组件 (`TagOptionSelector`)**：
  - **即时搜索与创建**：输入任意新标签名称，一键回车或点击「创建新标签」即可动态生成。
  - **8 种预置主题色动态切换**：支持为每个标签自由设置（紫色、蓝色、绿色、琥珀黄、玫瑰红、粉色、板岩灰、中性灰）。
  - **行内管理操作**：支持直接重命名标签文本、删除标签选项与动态解绑。
  - **双向选择支持**：单选（`select`）与多选（`multi_select`）模式全覆盖。
  - **智能防遮挡对齐**：根据视口与表格边缘自动计算位置（靠近右侧向左对齐、靠近底部预留内边距），彻底杜绝下拉菜单被截断遮挡。
- **默认空表创建**：新建多维数据表默认初始化为空数据，不再携带冗余 mock 数据。
- **行内输入缓冲优化**：引入 `TableTextInput` 与 `TableNumberInput` 本地缓冲输入组件，打字即时响应零卡顿，`onBlur` / `Enter` 统一提交，杜绝按键高频发起网络请求。
- **敏捷看板与字段 ID 生成**：新增列选项 ID 动态生成，避免跨列冲突；看板删除卡片增加二次确认弹窗防误触。

### 2. 🔒 笔记二次验证与 PBKDF2 校验系统性修复
- **修复密码校验分支误判 Bug**：彻底解决因哈希长度特征重叠导致合法 PBKDF2 密码被误分流进旧版 raw SHA-256 分支而报错「密码错误」的问题。
- **标准自描述哈希格式**：全面规范化为 `pbkdf2$600000${salt_hex}${hash_hex}`，600,000 次安全迭代 + 16 字节随机盐 + 常数时间比对，向下兼容历史加密数据并自动无感平滑升级。
- **端到端自动化复测**：全流程自动化测试覆盖「加锁 -> 错密拦截 -> 正确密码解密 -> 彻底解除密码锁定」，100% 通过验证。

### 3. ✨ AI 标签智能提炼与自动关联落地
- **万能格式解析器 (`parseTagsFromAIResponse`)**：全面兼容大模型输出的各种形态（标准 JSON 数组、Markdown 代码块、带前缀/后缀文本、`#标签` 格式、序号列表 `1. 标签`、符号列表 `- 标签`、顿号/逗号分隔等）。
- **即时合并与持久化**：提炼成功后自动合并至笔记当前标签列表并即时落库保存，提供清晰直观的 Toast 结果反馈。
- **提示词精准约束**：优化后端 AI 算子系统 Prompt，规范大模型输出格式。

### 4. 🪟 macOS 原生无边框窗口顶栏拖拽与双击缩放
- **Apple 原生 `performDrag` 调度**：通过 `WKScriptMessageHandler` 贯通网页端与 Swift 原生窗口管理器，点击顶部空白处按住即可无缝拖拽窗口。
- **智能交互元素过滤**：自动排除所有按钮、输入框、下拉菜单、链接等交互区域，防止误触发。
- **双击顶部最大化/还原**：双击顶部空白区域直接触发 macOS 系统原生 `window.zoom` 窗口缩放切换。

### 5. 📥 笔记批量导入与 macOS 文件选择器深度适配
- **原生文件选择器桥接 (`runOpenPanelWith`)**：修复 macOS WKWebView 原生环境下点击文件导入按钮无弹窗响应的系统级限制。
- **多格式与多编码批量导入**：支持 `.md`、`.markdown`、`.txt`、`.docx`、`.html` 及常见代码源文件批量导入，内置 UTF-8、GBK、GB18030、Big5 多字符集自动探测与转码。

### 6. 🗑️ 数据表回收站与快速删除协同
- **废纸篓查看与彻底删除/还原**：多维数据表全面接入统一回收站体系，支持在侧边栏悬浮一键归档删除，彻底删除与恢复链路闭环。

---

## 🚀 0820 V7 迭代更新日志 (macOS 桌面稳固、秒开启动与编辑体验全面优化)

### 1. 🍏 原生客户端秒开与启动体验
- **服务已就绪时零过渡直达编辑器**：打开 `Note.app` 先探测 `http://127.0.0.1:3000`，前端已在跑则直接进入笔记界面，不再每次闪出 Docker 启动页。
- **仅在服务未就绪时显示等待**：Docker Desktop 未唤醒或容器未起来时，才展示启动提示并后台拉起 `docker compose`。
- **工程路径自动记忆**：不再写死用户名路径；首次从仓库启动后写入 `~/Library/Application Support/LocalAINote/`，拷贝到「应用程序」仍能找到原工程。
- **AI 官方网页懒加载**：豆包 / DeepSeek / Kimi / Grok / Gemini 五个 WKWebView 改为点进对应智库再加载，降低冷启动内存占用。
- **窗口几何记忆**：关闭前自动保存窗口位置与尺寸，下次打开还原。
- **一键重编译**：新增 `./run.sh compile-app`（`scripts/compile_note_app.sh`），从 `scripts/main.swift` 重新编译仓库内 `Note.app`。

### 2. ⌨️ 原生菜单、快捷键与桌面交互
- **标准 macOS 菜单**：新建笔记、立即保存、查找、设置、折叠侧栏；重新加载改为 `⌘⇧R`，避免误刷新正在撰写的页面。
- **快捷键**：`⌘N` 新建、`⌘S` 立即保存、`⌘F` 搜索、`⌘P` / `/` 快速跳转、`⌘,` 设置、`⌘\` 折叠侧栏。
- **`⌘P` 命令面板**：快速搜索笔记、笔记本并执行常用操作。
- **可折叠侧栏**：窄屏下收成图标栏，宽度变化同步给原生内嵌 AI 视图。
- **拖拽归档**：中间栏笔记可直接拖入左侧笔记本分类。
- **暗色模式 / 上次打开的笔记 / 侧栏折叠状态本地持久化**。

### 3. ✍️ 编辑器保存与列表性能
- **切笔记 / 退出前强制落盘**：800ms 防抖保存之外，切换与卸载时立即 flush，避免丢掉最后几秒文字。
- **正文按 Markdown 落库**：Tiptap JSON 转为 Markdown 写入 `content`，全文搜索、摘要与反向链接不再只剩纯文本。
- **搜索输入防抖**：关键字 280ms 后再打接口，输入时不再每个字都全量刷新。
- **列表接口瘦身**：列表不再返回完整 `content_json` 与全文，改为摘要预览 + `content_length`；录音计数一次聚合，消灭 N+1。
- **侧栏计数独立接口**：`GET /api/notes/stats` 一次返回全部 / 收藏 / 废纸篓数量。
- **笔记本计数批量聚合**：分类树 `note_count` 不再逐本 `COUNT`。

### 4. 🗄️ 数据层、同步与备份
- **SQLite WAL + busy_timeout + 热路径索引**：自动保存、录音转写与同步不再轻易互相锁死。
- **配对状态落盘**：`data/sync_state.json` 持久化 Token 与已配对设备，Docker 重启后无需重新扫码。
- **设置页一键备份 / 恢复**：打包笔记库、录音与图片为 `.tar.gz` 下载；支持从备份覆盖恢复。
- **录音分析完成系统通知**（需授权）。
- **带 hash 的前端静态资源长期缓存**，HTML 仍禁止强缓存。

### 5. 📱 iOS 工程侧黑屏修复（未作为本轮发布重点）
- 补齐 Scene 窗口生命周期，避免真机安装成功后无窗口纯黑屏。
- `www` 以正确文件夹引用打进 Bundle，编译期自动拷贝静态资源。
- 自定义 `noteapp://` 协议加载前端，修复嵌套 `assets/` 路径与大文件发送截断。
- 加载失败时在屏幕上显示诊断信息，不再依赖 Xcode 控制台。

---

## 🚀 0820 V6 迭代更新日志 (macOS 端专属 Notion 式多维数据库表系统)

### 1. 📊 强类型属性字段系统 (Typed Properties)
- **多类型属性支持**：全面支持 **标题 (Title)**、**状态 (Status 胶囊)**、**单选/多选标签 (Select/Multi-Select)**、**日期选择器 (Date)**、**数字与百分比进度条 (Number / ProgressBar)**、**复选框 (Checkbox)**、**纯文本 (Text)**。
- **自定义列扩展**：支持一键「+ 新增列」、动态修改列名、删除列与自由调整列宽。

### 2. 📋 现代化表格视图 (Table View)
- **单元格行内编辑**：单击/双击即可在单元格中就地切换状态、选择标签、挑选日期与输入数值，告别繁琐弹窗。
- **底部智能计算汇总栏 (Calculation Bar)**：自动汇总统计当前总记录条数、数值列均值与求和、复选框完成百分比。
- **行操作与快速增删**：悬浮提供快速删除、复制与行详情直达。

### 3. 📌 敏捷看板视图 (Kanban / Board View)
- **泳道智能分组**：按「状态」或「单选标签」将数据行自动归类为可视化泳道列。
- **卡片流转与快捷添加**：卡片上直观展示优先级标签、截止时间与进度条，支持在泳道底部一键 `+ 快速添加` 卡片。

### 4. 📄 行即页面详情抽屉 (Record as a Page)
- **全尺寸编辑抽屉**：点击任意行的标题或「打开」按钮，右侧滑出专属详情抽屉。
- **属性与独立正文联动**：抽屉上方可集中查看与配置所有属性字段，下方提供专属的完整 Markdown 富文本长笔记编辑器。

## 🚀 0819 V5 迭代更新日志 (知识网络图谱、双向链接系统、闪念速记流与创作打卡热力图)

### 1. 🔗 块级双向链接系统 (`[[双向链接]]` 与反向引用面板)
- **快捷双链插入**：富文本编辑器工具栏新增 **`🔗 双链`** 按钮，点击唤出选择器即可快速搜索已有笔记并在正文插入 `[[目标笔记]]`。
- **内链徽章与秒级跳转**：正文中的所有 `[[...]]` 自动渲染为带有图标与主题背景色的高亮内链标签，macOS 与 iOS 触控点击均可瞬间跳转打开对应笔记。
- **反向引用面板 (Backlinks)**：每篇笔记正文底部新增 **「🔗 反向引用」** 折叠抽屉，自动检索并展示所有引用了当前笔记的其他文章列表及上下文摘要片段。

### 2. 🪐 交互式 2D 力导向知识关系图谱 (Knowledge Graph View)
- **原生高性能 Canvas 物理模拟引擎**：基于库伦斥力、胡克弹簧引力与向心阻尼算法，零繁重外部依赖，丝滑流畅 60fps 实时渲染。
- **全景星空网络漫游**：侧边栏新增 **「🕸️ 知识关系图谱」** 入口，将全库所有笔记关联、`[[双链]]` 与 `#标签` 渲染为星空网状图谱。
- **多维交互与手势支持**：支持鼠标滚轮与移动端手势捏合缩放（0.3x ~ 3.0x）、自由平移画布、拖拽节点物理重组、点击节点秒级跳转打开，并支持一键切换显示/隐藏 `#标签` 节点。

### 3. ⚡️ 闪念速记灵感流 (Floating Memo Stream)
- **极速碎片灵感捕捉**：侧边栏新增 **「⚡️ 闪念速记流」** 专属视图（参考 flomo / Memos 理念），支持输入一句话想法按 `Cmd + Enter` 即时捕捉记录。
- **智能标签提取与过滤**：正文中的 `#灵感`、`#会议`、`#随想` 等标签自动提取归类，支持顶部多标签一键筛选。
- **图片附件与置顶管理**：支持粘贴截图或点击上传图片，支持卡片置顶（📌）与删除管理。
- **✨ 一键批量合并为长笔记**：多选勾选碎片闪念，点击顶部 **「一键合并为长笔记」**，系统自动按时间线整理为包含小标题、图片与标签的结构化长篇 Markdown 笔记并归档。

### 4. 🟩 GitHub 风格 365 天知识产出打卡热力图 (Activity Heatmap)
- **产出追踪与习惯激励**：侧边栏底部新增 **「🔥 创作打卡」** 入口，一键呼出过去 365 天的绿色热力方块矩阵。
- **多指标统计**：实时汇总展示 **活跃天数**、**当前连续打卡天数**、**总字数沉淀量**，悬浮可查看具体日期产出的笔记篇数与字数。

### 5. 📱 macOS 与 iOS 双端离线引擎与局域网增量同步全面对齐
- **iOS 离线引擎升级**：升级移动端本地数据库引擎 `LocalAINote_iOS_DB` (IndexedDB v2)，支持在手机完全无网状态下离线计算图谱与打卡热力图。
- **局域网双向同步扩容**：双向增量同步协议将 `memos` 闪念数据纳入原子合并，同一 Wi-Fi 局域网下一键秒级对齐。

### 1. 📱 iOS 独立离线移动端 (Offline-First)
- **本地沙盒物理存储 (IndexedDB 引擎)**：iOS 端彻底脱离 macOS 服务端独立运行，所有笔记增删改查、富文本编辑、录音、标签、分类完全保存在 iPhone 本机沙盒中，离线无网状态下秒开秒存。
- **macOS 全功能 100% 对齐**：支持 Tiptap 所见即所得富文本、表格、4 风格润色、AI 续写、思维导图、5 大模型官方内嵌与二次密码验证加锁保护。
- **iOS Safari PWA 极速安装**：配置标准 `manifest.json` 与 Apple Touch Icon，在 Safari 中点击「添加到主屏幕」即可生成独立全屏的原生级体验。
- **iOS Native Xcode 工程**：在 `ios/NoteApp/` 目录下提供 Swift + WebKit + AVFoundation 独立移动端工程与麦克风、相机、局域网网络权限配置。
- **iOS 移动端界面深度适配**：集成 iOS Safe Area（顶部刘海/灵动岛与底部 Home Indicator 留白）、移动端响应式抽屉与触控友好交互。

### 2. ⚡️ 同一 Wi-Fi 局域网配对与双向原子数据同步 (LAN Sync)
- **免公网服务器，点对点受信任配对**：Mac 端自动检测局域网直连 IP 并生成高分辨率配对二维码与 6 位安全 PIN 码，iPhone 扫码或输入 PIN 码即可完成设备双向握手。
- **毫秒级增量双向同步 (Two-Way Sync)**：基于 `updated_at` 时间戳差异比对算法，仅推送与拉取变更增量数据，单次原子请求完成双向数据合并与智能冲突仲裁。
- **废纸篓与软删除同步**：修复软删除同步机制，两端删除与恢复操作毫秒级对齐。
- **快捷控制中心**：侧边栏左下角新增「⚡️ 多端同步」常驻入口，实时展示连接状态、最后同步时间与同步结果统计。

### 3. 🔒 系统性功能与安全审计深度加固 (18 项全量修复)
- **Token 防泄漏机制**：`/api/sync/info` 接口与二维码 Payload 彻底移除 Token 暴露，Token 仅在配对握手成功后加密返回。
- **防暴力破解与配对码动态轮换**：配对码升级为 6 位纯数字，每 5 分钟自动轮换过期；连续 5 次配对失败自动锁定 60 秒并强制重置配对码。
- **密码哈希网络传输隔离**：所有同步与拉取接口的 `password_hash` 字段统一过滤为 `null`，避免 PBKDF2 哈希暴露于网络，密码加锁各端独立管理。
- **IndexedDB 多 Store 单事务原子性**：修复 `bulkApplySyncData` 跨表写入逻辑，所有 ObjectStore 统一在单个原子事务中提交，彻底杜绝数据不一致。
- **CORS 局域网白名单限制**：CORS 由全开放 `*` 改为正则严格匹配本地与私有局域网 IP 段（`192.168.*`, `10.*`, `172.16-31.*`），彻底防范公网恶意网页 CSRF。
- **DoS 流量防护**：同步接口增加单次最大 500 篇笔记 / 100 个笔记本的数据量限制。
- **iOS ATS 权限精细化与内存泄露修复**：禁用全局 `NSAllowsArbitraryLoads`，改用 `NSAllowsLocalNetworking` 仅放行局域网 HTTP；使用弱引用代理修复 WKScriptMessageHandler 循环引用内存泄漏。
- **恢复 Favicon 视网膜图标与 UUID 安全生成**：恢复 `<link rel="icon">` 网页与 macOS 客户端图标，UUID 生成升级为浏览器原生 `crypto.randomUUID()`。

## 🚀 0819 V2 迭代更新日志 (安全加固与交互升级)

### 1. 🔒 企业级密码哈希与核心安全加固
- **PBKDF2-HMAC-SHA256 加密升级**：密码算法全面升级为 **600,000 次安全迭代** 与 16 字节随机盐（符合 OWASP 2023 行业安全标准），彻底抵御 GPU 暴力破解与彩虹表。
- **平滑向后兼容与自动无感迁移**：数据库现有笔记自动兼容旧格式，用户在下次输入密码验证通过后自动静默升级为 PBKDF2。
- **API Key 凭据防泄漏脱敏**：AI 设置接口返回值全面采用严格掩码脱敏（如 `sk-...360c`），网络传输与前端存储绝不暴露明文凭据。
- **克隆笔记密码隔离**：克隆后的副本笔记默认解除加锁状态，杜绝密码交叉共享与越权查看风险。
- **批量导入防 DoS 防护**：增加单次最多 100 个文件、单文件最大 10MB 的校验防护，拦截非法格式与超大文件。

### 2. 🎯 下拉菜单交互体验深度重构
- **点击锁定展开机制 (Click-to-Toggle)**：重构表格工具、多风格智能润色、多格式导出 3 大下拉菜单，由 CSS Hover 改为点击稳固展开，彻底消除鼠标移动时菜单意外消失（选不到）的痛点。
- **点击外部自动收起 (Click Outside)**：支持全局点击页面空白处自动关闭浮层，点击选项后立即执行并平滑收起。

### 3. 🎨 AI 智库品牌视觉与布局精细化
- **5 大品牌官方矢量 SVG Logo**：全面上线豆包、DeepSeek、Kimi、Grok、Gemini 官方标准高保真矢量图标，Retina 高清视网膜屏幕呈现极佳质感。
- **侧边栏层级向右缩进**：AI 智库子菜单增加向右缩进（`pl-3`），与笔记本分类树形成协调统一的二级层级视图。

## 🚀 0819 V1 迭代更新日志 (功能大版本落地)

### 1. 📝 富文本所见即所得编辑器全要素升级
- **复杂表格 (Table)**：集成 Tiptap Table 模块，支持插入 3x3 响应式表格、动态增删行与列、整表删除。
- **排版要素扩展**：新增水平分割线 (`HorizontalRule`)、删除线 (`Strike`) 与下划线 (`Underline`)。
- **一键克隆/复制笔记副本**：支持一键生成当前笔记完整副本（包含正文、富文本 JSON 结构与标签）。
- **沉浸式阅读 / 编辑模式切换**：支持一键切换极简阅读视图（隐藏编辑条与标签，纯净只读）与编辑视图。

### 2. ✨ 第一层 AI 沉浸编辑辅助 (Copilot) 深度增强
- **四风格多维智能润色**：新增下拉多风格润色选项（💼 商务正式 / ⚡️ 极简精炼 / 💬 轻松口语 / 🎓 学术专业）。
- **⚡️ 光标位置 AI 续写 (Context-Aware Continuation)**：智能感知光标前文语境，从当前光标处无缝流式打字机插入后续内容。
- **🧹 杂乱草稿一键 AI 格式化**：将零碎草稿、会议记录碎片一键重构为层级鲜明的 Markdown 笔记。
- **🔍 智能语法与错别字纠错**：一键智能校对并自动修正错别字与语病。
- **💬 选中文本气泡浮动菜单 (Bubble Menu)**：选中文本时自动弹出悬浮工具条（加粗、斜体、润色、扩写、纠错）。

### 3. 📂 导入导出与数据互通工作流
- **📥 批量导入**：侧边栏新增导入入口，支持本地多选 `.md` 与 `.txt` 文件批量解析入库。
- **📤 多格式导出**：支持导出为 Word (.docx)、Markdown (.md)、内置样式 HTML (.html) 与纯文本 (.txt)。

### 4. 📅 每日日志 (Daily Notes) 极速工作流
- 侧边栏新增「📅 每日日志」专属入口，一键定位或自动以标准模版新建当天日期的日志卡片。

### 5. 🤖 AI 咨询与智库全景升级
- **折叠面板交互**：新增「🤖 AI 咨询与智库」展开与折叠控制，与笔记本分类树保持一致。
- **五大官方 AI 智库原生内嵌**：原生集成 **豆包**、**DeepSeek**、**Kimi**、**Grok** 与 **Gemini** 官方网页版。
- **macOS 多顶级 WKWebView 原生持久化免登**：基于独立顶级原生 WebKit 架构，共享系统级持久沙盒存储，永久保持登录态，告别重复扫码。

## 🛠 快速启动与部署指南

- **方案 1：Docker Compose 一键启动 (推荐)**
  ```bash
  docker-compose up -d
  ```
- **方案 2：macOS 原生桌面应用启动**
  ```bash
  ./run.sh app
  # 或安装到应用程序：
  ./run.sh install-app
  ```
- **方案 3：本地源码开发环境启动**
  ```bash
  # 后端
  python main.py
  # 前端
  npm run dev
  ```

## 📦 项目技术栈与依赖清单

- **前端**：React 18, Vite, TailwindCSS, Tiptap, Lucide Icons, Mermaid, KaTeX.
- **后端**：Python 3.11, FastAPI, SQLAlchemy, SQLite, Uvicorn, python-docx, Pydantic.
- **客户端**：Swift, WebKit, Cocoa, AVFoundation.

## 🛡️ 隐私与数据安全说明

- 数据 100% 物理留存于 `./data/` 目录；
- API Key 与敏感环境变量存放在本地 `.env`，不上传云端；
- 加锁笔记强脱敏隔离，解密状态内存级管理。

## 💻 常用维护命令 (`./run.sh`)

```bash
./run.sh app             # 打开 macOS 原生独立桌面应用窗口
./run.sh install-app     # 安装应用到启动台
./run.sh start           # 启动服务
./run.sh stop            # 停止服务
./run.sh restart         # 重启服务
./run.sh status          # 查看运行状态
./run.sh logs            # 实时查看运行日志
./run.sh backup          # 备份数据
./run.sh restore <file>  # 从备份压缩包恢复数据
```
